{{- if .Values.rbac.create -}}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "kube-user-manager.fullname" . }}
  labels:
    {{- include "kube-user-manager.labels" . | nindent 4 }}
rules:
  # 核心资源
  - apiGroups: [""]
    resources: ["serviceaccounts", "secrets", "namespaces"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["create", "patch"]
  # RBAC 资源 - 需要escalate权限来创建/更新任何ClusterRole
  - apiGroups: ["rbac.authorization.k8s.io"]
    resources: ["clusterroles", "rolebindings", "clusterrolebindings", "roles"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "bind", "escalate"]
  # 为了能够通过rolebinding授予权限，operator需要拥有所有可能被授予的权限
  # 或者使用escalate权限（但escalate需要搭配实际权限才能工作）
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: ["*"]
  # CRD 资源
  - apiGroups: ["apiextensions.k8s.io"]
    resources: ["customresourcedefinitions"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]
  # 自定义资源
  - apiGroups: ["osip.cc"]
    resources: ["lensuser", "luconfig"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: ["osip.cc"]
    resources: ["lensuser/status", "luconfig/status"]
    verbs: ["get", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "kube-user-manager.fullname" . }}
  labels:
    {{- include "kube-user-manager.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "kube-user-manager.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "kube-user-manager.serviceAccountName" . }}
    namespace: {{ .Values.namespace }}
{{- end }}

